// объявление массивов (не именнованных коллекций):
/*
let arr1 = new Array();
let arr2 = [];

let users = ["Anthony", "Andrew", "Anna"];
console.log(users);

console.log(users[0]);

users[1] = "Thomas";
console.log(users);

users[3] = "Andy";
console.log(users);

console.log(users.length);
users[4] = 5;
console.log(users);


// очередь и стек:

// с концом строки работает быстрее
users.pop(); // удаляем последний элемент и возвращаем его значение
console.log(users);

users.push("Richard"); // вставляем в конец массива и возвращаем полученную длинну массива
console.log(users);


// с началом строки работает медленнее
users.shift(); // удаляем первый элемент и возвращаем его значение
console.log(users);

users.unshift("Elsa"); // вставляем в начало массива один и более элементов и возвращаем полученную длинну массива
console.log(users);


// работа с циклами:
for (let i = 0; i < users.length; i++) {
    console.log(users[i]);
}

for (let user of users) {
    console.log(user);
}


// удаление элементов с помощью обнуления длинны:
users.length = 0
console.log(users);

// многомерные массивы:
let matrix = [
    [10, 9, 8],
    [7, 6, 5],
    [4, 3, 2]
];

console.log(matrix);
console.log(matrix[0][1]);
*/
